apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: local-0
  name: local-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
