apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: local-1
  name: local-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
